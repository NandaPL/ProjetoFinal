import pygame

pygame.init()
screen = pygame.display.set_mode((600,350))
pygame.display.set_caption('Projeto Final')
pygame.display.flip()

run = init = True

while run:
    if init:
        screen

    pygame.display.update()